<h1>Results</h1>

<% if @clothings.any? %>
  <% @clothings.each do |clothing| %>
    <div>
      <%= link_to clothing.name, clothing_path(clothing) %>
      <p><%= clothing.brand %> — <%= clothing.material %></p>
    </div>
  <% end %>
<% else %>
  <p>Nothing</p>
<% end %>